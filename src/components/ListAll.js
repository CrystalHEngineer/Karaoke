import React, {useEffect, useState} from 'react';
import {Link, navigate} from '@reach/router';
import axios from 'axios';

const ListAll = (props) => {
    return (
        <div>
            <h1>List all component</h1>
        </div>
    )
};

export default ListAll;